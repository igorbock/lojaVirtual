@model LojaVirtual.Web.Models.ProdutoViewModel

@using LojaVirtual.Web.HtmlHelpers

@{
    ViewBag.Title = "ListaProdutos";
}

<h2>ListaProdutos</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@foreach (var item in Model.Produtos) {
    @Html.Partial("ProdutoPartialView", item) 
 }
<div>@Html.PageLinks(Model.Paginacao, x => Url.Action("ListaProdutos", new { pagina = x})) </div>
